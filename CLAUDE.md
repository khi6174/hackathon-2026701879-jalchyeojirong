# 프로젝트 개요

🏸 배드민턴 동아리 활동 기록 웹앱

동아리 임원이 활동 내역을 등록하고 참여자를 명부에서 선택해, "누가 얼마나 참여했는지"를 추적하는 웹앱.

팀: 2026701879 / 잘쳤지롱  
팀원: 김용우 (1인 팀)

# 기술 스택

- HTML / CSS / JavaScript (프레임워크 없음)
- 데이터 저장: 브라우저 localStorage
- 외부 라이브러리: 없음 (순수 vanilla JS)

# 파일 구조

- `index.html` : 화면 구조
- `style.css`  : 스타일 (반응형)
- `app.js`     : 기능 로직 (회원/활동 관리, 검증, 통계, 매칭)
- `CLAUDE.md`  : 작업 규칙 (이 파일)
- `README.md`  : 제출용 안내

# 코딩 규칙

- 함수와 변수 이름은 영어 camelCase, 의미가 드러나게 작성
- 주요 함수 위에는 한 줄 한글 주석을 단다
- 하나의 함수는 하나의 일만 한다
- 들여쓰기는 스페이스 2칸
- 사용자 입력은 반드시 `textContent`로 렌더 (XSS 방지)
- 날짜는 로컬 자정 기준으로 비교 (UTC 파싱 버그 회피)

# 데이터 구조

**회원** (localStorage 키: `members`)  
`{ id, name, skillLevel, createdAt }`  
skillLevel: "상" | "중" | "하"

**활동** (localStorage 키: `activities`)  
`{ id, title, date, place, participantIds, memo, createdAt }`  
participantIds: 회원 id 배열 → 참여 인원수는 `.length`로 파생

# 작업 방식

- 코드를 작성하기 전에 무엇을 어떤 순서로 만들지 먼저 제안한다
- 한 번에 하나의 기능만 구현한다. 여러 기능을 동시에 만들지 않는다
- 기존에 동작하던 코드를 요청 없이 수정하거나 삭제하지 않는다
- 확실하지 않은 부분은 추측하지 말고 먼저 질문한다

# 하지 말 것

- 서버, 데이터베이스, 빌드 도구 도입 금지
- 요청하지 않은 파일 새로 생성 금지
- 사용하지 않는 예시 코드나 더미 데이터 대량 생성 금지
- 한 번에 100줄 넘는 코드를 통째로 출력하지 않기

# 커밋 규칙

- 기능 하나가 동작하면 바로 커밋한다
- 커밋 메시지 형식: `<기능명>: <무엇을 했는지>`
  예) `회원등록: 회원 등록 폼과 저장 기능 구현`

# 현재 진행 상황

- [x] 저장소 초기화 & CLAUDE.md (커밋: 3065f4c)
- [x] index.html 기본 구조 + style.css 뼈대 (커밋: e86fb3a)
- [x] 회원 명부 등록/목록 (커밋: e86fb3a)
- [x] 활동 등록 + 검증 (참여자 체크박스 포함) (커밋: e86fb3a)
- [x] 활동 목록 조회/삭제 + 통계 + 참여 랭킹 (커밋: e86fb3a)
- [x] 검색 + 수정 + 샘플 데이터 (커밋: e86fb3a)
- [x] 반응형 레이아웃 (커밋: e86fb3a)
- [x] 1vs1/2vs2 균형 매칭 (커밋: f04ed2c)
- [x] 자체검증 함수 + README + 최종 테스트
- [x] 활동별 매칭 UX 개선 - 모달 UI (커밋: 714bf9d)
- [x] 매칭 UX 최종 개선 - 회원 선택 매칭 통합 (커밋: 9cf6923)

# 범위 밖 (의도적 제외)

- 기기 간 / 사용자 간 데이터 공유 (localStorage 한계)
- 회원 삭제 (참조 무결성 복잡도)
- 토너먼트 대진표 / 경기 결과 / 승패 추적
